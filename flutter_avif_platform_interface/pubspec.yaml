name: flutter_avif_platform_interface
description: Common platform interface for the flutter_avif plugin.
version: 3.1.1
repository: https://github.com/joernto/flutter_avif/tree/main/flutter_avif_platform_interface
publish_to: none

environment:
  sdk: ^3.10.0
  flutter: ">=3.22.0"

dependencies:
  ffi: ^2.1.3
  protobuf: ^6.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  ffigen: ^20.1.1